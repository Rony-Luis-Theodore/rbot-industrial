"""
=============================================================================
R-Bot Backend — Paquete principal de la aplicación
=============================================================================

Propósito:
    Marca el directorio `app/` como paquete Python importable.

Conexión con el resto:
    Todos los módulos internos (api, core, domain, services, adapters)
    se importan bajo el prefijo `app.*`.
=============================================================================
"""
