[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lab-map"
version = "1.0.0"
description = "Robust Occupancy lab SE2 + geometry for R-Bot Industrial"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
